apiVersion: v1
kind: Service
metadata:
  name: xrp-collector
  namespace: default
  labels:
    app: xrp-collector
spec:
  selector:
    app: xrp-collector
  ports:
    - port: 80
      targetPort: 8080
      name: http
  type: ClusterIP