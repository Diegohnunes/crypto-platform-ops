apiVersion: v1
kind: Service
metadata:
  name: crypto-frontend
  namespace: default
spec:
  selector:
    app: crypto-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 4000
  type: ClusterIP
