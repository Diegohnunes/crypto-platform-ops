apiVersion: v1
kind: Service
metadata:
  name: bnb-collector
  namespace: default
  labels:
    app: bnb-collector
spec:
  selector:
    app: bnb-collector
  ports:
    - port: 80
      targetPort: 8080
      name: http
  type: ClusterIP